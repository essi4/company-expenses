import { NextResponse } from "next/server";
import { getCompanyContext } from "@/lib/company";
export const dynamic="force-dynamic";
const allowed=new Set(["suppliers","categories","accounts","budgets","checks","purchase_items"]);
export async function GET(req:Request){const c=await getCompanyContext();if(!c.companyId)return NextResponse.json({success:false,message:"دسترسی غیرمجاز"},{status:401});const type=new URL(req.url).searchParams.get("type")||"";if(!allowed.has(type))return NextResponse.json({success:false,message:"نوع نامعتبر"},{status:400});const {data,error}=await c.supabase.from(type).select("*").eq("company_id",c.companyId).order("created_at",{ascending:false});if(error)return NextResponse.json({success:false,message:error.message},{status:500});return NextResponse.json({success:true,data:data||[]},{headers:{"Cache-Control":"private, no-store"}})}
export async function POST(req:Request){const c=await getCompanyContext();if(!c.companyId)return NextResponse.json({success:false,message:"دسترسی غیرمجاز"},{status:401});const body=await req.json();const type=String(body.type||"");if(!allowed.has(type))return NextResponse.json({success:false,message:"نوع نامعتبر"},{status:400});delete body.type;delete body.company_id;const {data,error}=await c.supabase.from(type).insert({...body,company_id:c.companyId}).select().single();if(error)return NextResponse.json({success:false,message:error.message},{status:400});return NextResponse.json({success:true,data})}
export async function PUT(req:Request){const c=await getCompanyContext();if(!c.companyId)return NextResponse.json({success:false,message:"دسترسی غیرمجاز"},{status:401});const body=await req.json();const type=String(body.type||"");const id=Number(body.id);if(!allowed.has(type)||!id)return NextResponse.json({success:false,message:"ورودی نامعتبر"},{status:400});delete body.type;delete body.company_id;delete body.id;const {data,error}=await c.supabase.from(type).update(body).eq("id",id).eq("company_id",c.companyId).select().single();if(error)return NextResponse.json({success:false,message:error.message},{status:400});return NextResponse.json({success:true,data})}
export async function DELETE(req:Request){const c=await getCompanyContext();if(!c.companyId)return NextResponse.json({success:false,message:"دسترسی غیرمجاز"},{status:401});const body=await req.json();const type=String(body.type||"");const id=Number(body.id);if(!allowed.has(type)||!id)return NextResponse.json({success:false,message:"ورودی نامعتبر"},{status:400});const {error}=await c.supabase.from(type).delete().eq("id",id).eq("company_id",c.companyId);if(error)return NextResponse.json({success:false,message:error.message},{status:400});return NextResponse.json({success:true})}
